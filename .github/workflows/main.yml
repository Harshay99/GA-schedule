name: Scheduling a Workflow

on:
  schedule:
    - cron: '50 12 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:

      - name: Run a script
        run: echo "This is a scheduled action!"
